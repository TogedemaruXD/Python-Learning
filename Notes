Functions:
  Functions do a set of code when asked
  In between the () are parameters
  Parameters are what you bring inside a function
  To bring a variable outside of a function. you need to say return at the end
  To call a function you need to say a function's name
  If you have a function that returns a variable you need to call it with a variable
